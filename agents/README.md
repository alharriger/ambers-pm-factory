# Agents

One folder per agent, built one at a time in the dependency order from pm-factory-strategy.md §5. Every agent ships as a matched set: SKILL.md (generator, emits artifact + claims ledger), verifier spec, versioned rubric, golden corpus (10-20 cases including must-fails), changelog. No agent lands here without plugging into the harness. Empty until Phase 2.
