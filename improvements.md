# Improvement Backlog

The system's learning queue. Gaps surfaced by verification gates, Amber's feedback, or the system itself land here as proposed updates; nothing applies without Amber's confirm (invariant 9), and every applied update gets a changelog entry in the doc or agent it touched. decisions.md records *what was decided*; this file tracks *what should change next*.

Entry format: **date · gap · surfaced by** (gate failure | Amber feedback | self-identified) **· proposed update · status** (Proposed | Approved | Applied | Rejected).

---

- **2026-07-19 · `identity/README.md` "known gap" paragraph is stale** (resume.md and evals/ now exist) · self-identified · update the README's contents list and delete the gap paragraph · **Proposed**
- **2026-07-19 · `identity/amber_voice_guide.md` points at `context/writing-eval-system.md`**, a career-project path · self-identified · repoint to `identity/writing-eval-system.md` (or leave until Phase 5 migration if the career project stays canonical) · **Proposed**
- **2026-07-19 · `identity/evals/resume-evals.md` references `amber-resume-SKILL-updated.md`**, absent from this repo · self-identified · pull the file in or annotate the reference as career-project-only · **Proposed**
- **2026-07-20 · Committed prose predates the public-portfolio decision** · Amber feedback (repo stays public) · one-time redaction review before first push; scan found 5 spots, each replaceable with a category reference that keeps the doc's meaning: (1) CLAUDE.md:40 names the two factual-boundary health domains → "the factual-boundary domains listed in `identity/`"; (2) strategy §3.3 same; (3) strategy §2 and (4) KICKOFF Phase 1 step 6 name specific past failures (a metric error, an invented team, a named company's draft) → describe by failure *type* and point the corpus at gitignored `evals/must-fail/`; (5) strategy §7 references personal rehab logs as an evidence source → "personal training logs". Amber approves each · **Proposed**
- **2026-07-20 · No code-enforced privacy gate** — the public repo relies on a human remembering to scan every staged diff, which invariant 2 says is exactly the wrong actor for a checkable rule · self-identified · pre-commit hook: block any staged file under a personal path, and scan the staged diff for a maintained deny-list of personal terms (names, employers, health domains, private product identifiers) sourced from a gitignored config so the deny-list itself is not public · **Proposed — recommend building first in Phase 0.5**
- **2026-07-20 · Adoptability: another person cannot yet run this factory on their own files** · Amber feedback (stated future goal, low priority now) · commit an `identity/TEMPLATE/` blank pack (empty files with the required sections and a README explaining what each supplies) so the interface is public while the content stays private; keep every identity-dependent component referencing files by role, never inlining Amber-specific content, so this stays cheap · **Proposed — deferred, revisit after Phase 1**
